# 农机Agent调度·专业技术说明文档

## 1. 文档目的

本文档从技术实现视角说明当前系统的结构、核心模块、关键接口、Agent 逻辑实现方式、天气与地图联动、测试验证与后续扩展方向。

文档内容以当前仓库实际代码为准，不描述尚未落地的理想化方案。

## 2. 技术目标

当前系统的技术目标不是做大规模分布式平台，而是构建一个可运行、可演示、可扩展的 Agent 驱动调度原型。该原型需要同时满足：

- 后端接口可运行
- 状态机可校验
- 前端交互可视化
- Agent 决策链路可解释
- 异常场景可复现

## 3. 技术栈

当前项目采用如下技术栈：

- 后端框架：FastAPI
- ORM：SQLAlchemy
- 数据库：SQLite
- 前端：原生 HTML + CSS + JavaScript，无构建依赖
- 地图：高德地图 JavaScript API
- 天气：Open-Meteo 免费天气接口
- LLM 接入：OpenAI 兼容接口 + SSE 流式转发
- 测试：Python `unittest`

这套技术栈的选型理由是：

- 学习和讲解成本低
- 部署简单
- 本地可快速复现
- 适合做规则引擎 + Agent 演示型系统

## 4. 系统结构

项目目录可以分为三层：

- `backend/`：后端接口、状态机、数据库模型、Agent 逻辑
- `frontend/`：演示页面、地图、天气卡片、LLM 交互和一键演示脚本
- 根目录数据与文档：模拟数据、方案文档和说明材料

后端核心结构包括：

- `app/main.py`：FastAPI 启动入口与路由挂载
- `app/models.py`：数据库模型定义
- `app/schemas.py`：Pydantic 输入输出模型
- `app/state_machine.py`：订单状态机
- `app/routers/`：业务接口路由
- `app/services/agent_logic.py`：派单和异常处理规则逻辑
- `app/services/weather_service.py`：天气获取、风险计算和演示天气覆写

前端核心结构包括：

- `frontend/index.html`：页面结构
- `frontend/styles.css`：样式
- `frontend/app.js`：页面状态、接口调用、地图渲染、演示脚本

## 5. 数据模型设计

系统核心数据模型定义在 `backend/app/models.py`，主要包括：

- `User`：用户基础信息，包含角色字段
- `Machine`：农机资源，包括位置、状态、健康分
- `ServiceSKU`：服务商品，与农机关联
- `Order`：订单主表
- `DispatchLog`：派单日志
- `OrderEvent`：订单状态变化和事件轨迹
- `TelemetryPoint`：遥测点位预留表
- `WeatherAlert`：天气预警预留表
- `Review`：评价信息

技术上，这套模型完成了从“交易实体”到“决策实体”的映射，为 Agent 的派单、异常处理和审计提供了数据基础。

## 6. 订单状态机实现

订单状态机定义在 `backend/app/state_machine.py`。

当前状态机的核心特点是：

- 强约束：每一步状态转换必须合法
- 可枚举：每个状态的合法下一状态明确定义
- 可测试：测试中显式验证合法与非法流转

例如：

- `CREATED -> PAID_ESCROW` 合法
- `DISPATCH_CONFIRMED -> IN_SERVICE` 合法
- `IN_SERVICE -> ABNORMAL_PENDING` 合法
- `ABNORMAL_PENDING -> IN_SERVICE` 合法，表示天气恢复后继续作业
- `ABNORMAL_PENDING -> REFUNDING -> REFUNDED` 合法，表示异常终止后退款

状态机的技术价值在于：

- 防止前端误操作
- 保证演示流程稳定
- 给业务解释提供统一依据

## 7. Agent 核心实现方式

当前 Agent 并不是一个完全自由生成的黑盒智能体，而是“规则引擎 + LLM 辅助”的混合实现。

### 7.1 需求解析

需求解析逻辑入口在：

- 后端：`/api/agents/demand/parse`
- 前端实际演示：优先通过 LLM 流式解析

当前策略是：

- 如果接入外部 LLM，则由 LLM 负责把自然语言转成结构化 JSON
- 后端也保留了规则解析函数 `parse_demand_text` 作为基础能力

### 7.2 派单建议

派单逻辑在 `backend/app/services/agent_logic.py` 的 `build_dispatch_proposal`。

主要处理过程如下：

1. 根据订单 `sku_id` 找到对应服务类型。
2. 选出同一作业类型下处于可用状态的候选农机。
3. 计算每个候选机具的多维分数。
4. 按总分降序排序。
5. 输出推荐机具、候选列表、置信度和理由。

当前评分包含以下维度：

- 距离分
- 可用性分
- 机主口碑分
- 价格分
- 天气风险分

当前权重大致为：

- 距离：0.28
- 可用性：0.22
- 口碑：0.18
- 价格：0.15
- 天气风险：0.17

技术上这是一个确定性规则引擎，优点是稳定、可控、便于讲解。

### 7.3 异常处理

异常处理逻辑在 `handle_exception`。

当前支持事件类型包括：

- `fault`
- `weather`
- `timeout`
- `user_change`

其中：

- `fault` 主要触发改派候选重算
- `weather` 主要结合目的地天气与农机所在地天气做暂停或改派判断

### 7.4 决策解释

决策解释支持两层：

- 规则层的简短解释
- LLM 层的面向用户自然语言解释

前端通过 `/api/llm/chat/stream` 实现流式输出，可将派单或异常决策转为需求方可读文本。

## 8. 天气能力实现

天气能力由 `backend/app/services/weather_service.py` 和 `backend/app/routers/weather.py` 共同完成。

### 8.1 基础天气能力

系统通过 Open-Meteo 获取当前天气和未来 6 小时天气数据，并基于以下指标计算风险：

- 最大降水概率
- 最大降水量
- 最大风速
- 最坏天气码

再进一步输出：

- `risk_level`
- `risk_score`
- `dispatch_penalty`
- `recommendation`

### 8.2 目的地与农机所在地天气比较

天气逻辑不是只查“订单目的地”，而是支持比较：

- 农机当前所在地天气
- 订单目的地天气

比较结果会输出：

- 风险差异级别
- 建议动作
- 建议原因

当前核心规则是：

- 目的地高风险时优先暂停
- 农机所在地比目的地明显更好，且路程较远时，倾向改派到更近资源
- 中等风险时先观察或谨慎执行

### 8.3 演示天气覆写

为了避免依赖真实天气导致演示不稳定，系统实现了“演示天气覆写”机制。

相关接口在：

- `POST /api/weather/demo/order/{order_id}/scenario`
- `DELETE /api/weather/demo/order/{order_id}/scenario`
- `DELETE /api/weather/demo/overrides`

技术上这是一个内存级覆写仓库，允许对订单目标点和当前执行农机位置注入：

- `clear`
- `watch_rain`
- `severe_rain`
- `severe_storm`

这样可以稳定复现：

- 目的地晴朗
- 目的地下雨
- 目的地暴雨/雷暴
- 农机所在地晴朗但目的地恶化

## 9. 前端交互与地图联动

前端全部逻辑集中在 `frontend/app.js`。

### 9.1 页面状态管理

虽然没有使用前端框架，但页面维护了统一的 `state` 对象，用于管理：

- 用户、农机、SKU、订单列表
- 当前选中订单
- 当前天气
- 最近一次派单建议
- 最近一次异常处置结果
- LLM 配置
- 地图运行态

### 9.2 地图联动

地图接入高德 JS API，当前支持展示：

- 当前订单目的地
- 全部农机位置
- 候选农机连线
- 当前选中农机
- 故障机具路线
- 目的地天气风险角标

在天气和异常场景中，地图承担了“把决策变得直观”的任务。

### 9.3 天气面板交互

前端天气面板目前具备以下动作：

- `天气自动处置`
- `天气复检`
- `恢复原机继续`
- `取消并退款`

对应技术逻辑如下：

- 自动处置：调用异常处理接口，让 Agent 先给出 `pause` 或 `reassign`
- 天气复检：重新拉取订单天气
- 恢复继续：执行 `ABNORMAL_PENDING -> IN_SERVICE`
- 取消退款：执行 `ABNORMAL_PENDING -> REFUNDING -> REFUNDED`

## 10. 一键演示脚本实现

前端实现了三个一键演示流程：

- 标准派单演示
- 天气暂停演示
- 故障改派演示

技术上，这三个脚本本质是顺序调用一组接口动作，并在每一步之间插入固定延迟和日志输出。

当前固定延迟为 5 秒，目的是：

- 避免演示过快看不清
- 便于边播报边讲解

其中天气暂停演示已经补成完整闭环：

1. 标准派单并开工
2. 注入目的地暴雨/雷暴
3. Agent 自动暂停
4. 注入天气恢复
5. 天气复检
6. 恢复原机继续
7. 完成作业并验收

## 11. LLM 流式网关实现

系统通过 `/api/llm/chat/stream` 接入 OpenAI 兼容接口，并将上游 SSE 数据转为前端可消费的流。

技术特点包括：

- 支持自定义 URL、API Key、模型名
- 支持 DeepSeek 等 OpenAI 兼容模型
- 前端使用 `fetch + ReadableStream` 解析流
- 主要用于需求解析与决策解释

当前这样设计的原因是：

- 让认知理解和解释能力由 LLM 提供
- 把高稳定性的派单和状态推进保留给规则引擎

## 12. 测试与验证

当前后端测试基于 `unittest`，已经覆盖：

- 状态机合法与非法流转
- 标准订单从创建到完成
- 故障异常改派
- 天气异常自动暂停
- 异常后恢复继续
- 异常后退款完成
- 天气演示覆写场景

当前已通过的验证包括：

- 后端单元测试通过
- 前端 `app.js` 语法检查通过
- 后端代码 `compileall` 通过

这意味着当前系统不只是“能点通一次”，而是有一定回归保障。

## 13. 设计上的技术取舍

当前实现有几个刻意的取舍：

### 13.1 为什么不用复杂前端框架

因为当前目标是验证业务链路和 Agent 逻辑，而不是做前端工程化展示系统。

### 13.2 为什么派单核心仍然是规则引擎

因为演示场景需要稳定和可解释。

如果直接让 LLM 决定派单，可能会出现：

- 同样输入结果波动
- 难以复现
- 难以解释评分依据

### 13.3 为什么加入演示天气覆写

因为实时天气不可控，不适合做现场演示。

### 13.4 为什么保留人工确认

因为派单、改派、退款都属于高风险动作，业务上不应完全交给模型自动执行。

## 14. 后续可扩展方向

从当前技术基线继续扩展，建议优先方向包括：

- 引入 PostgreSQL 或 MySQL，替代 SQLite
- 将天气覆写改为持久化场景配置，而非内存数据
- 引入更完整的遥测模拟和地理围栏判断
- 将 LLM 接入从“解释/解析”扩展到“结构化决策建议”
- 将前端改造为 Vue 或 React 版本，提升组件化能力
- 增加审计页和异常复盘页

## 15. 技术层结论

当前系统已经具备一个可运行 Agent 调度系统应有的核心技术特征：

- 有清晰后端边界
- 有强约束状态机
- 有规则引擎与 LLM 的职责分工
- 有地图与天气可视化
- 有可重复的演示异常场景
- 有基础回归测试

从技术实现角度看，这个系统不是概念原型，而是一个已经跑通业务闭环、具备后续扩展基础的最小可行系统。
