# 农机Agent调度·业务逻辑说明文档

## 1. 文档目的

本文档从业务视角说明当前系统的目标、参与角色、订单流转逻辑、Agent 介入点和三类核心演示场景，便于方案汇报和业务讲解。

当前系统不是在做一个“大而全的农机平台”，而是在验证一件更核心的事情：在农机服务电商场景下，Agent 是否能够理解需求、辅助派单、应对异常并提供可解释的决策支持。

## 2. 业务目标

系统面向的是“农机服务电商调度”场景，目标是让需求方能够像下电商订单一样购买农机服务，并由 Agent 在后台参与调度与履约协同。

当前系统重点验证三类业务能力：

- 需求理解能力：把自然语言需求转成结构化订单要素。
- 调度决策能力：为订单推荐合适的农机并给出理由。
- 异常处置能力：当天气或设备异常出现时，辅助做暂停、改派或恢复处理。

## 3. 业务角色

系统中主要有四类业务角色：

- 需求方：发起农机服务需求、支付托管、确认验收。
- 机主：提供农机服务资源，承接订单并执行作业。
- 调度员：确认派单、处理异常、确认改派或恢复。
- Agent：不直接替代人类责任主体，但对需求理解、派单建议、异常处置、解释输出进行智能辅助。

## 4. 业务对象

当前业务围绕以下核心对象展开：

- 用户：包括需求方、机主、调度员。
- 农机：包含机型、位置、状态、健康分等属性。
- 服务SKU：机主可售卖的具体服务商品，如收割、耕整、植保等。
- 订单：服务交易主线，承载金额、目标位置、执行状态和履约过程。
- 派单日志：记录 Agent 的候选计算和建议结果。
- 订单事件：记录状态流转轨迹，保证可追溯。
- 天气信息：用于判断目的地是否适宜继续作业。

## 5. 整体业务闭环

标准业务闭环如下：

1. 需求方输入自然语言需求。
2. Agent 将需求解析为结构化字段。
3. 系统基于服务SKU创建订单。
4. 需求方进入支付托管。
5. Agent 生成派单候选与推荐结果。
6. 调度员确认派单。
7. 农机开始作业。
8. 作业完成后进入待验收。
9. 需求方确认验收。
10. 订单完成并沉淀为可追溯事件记录。

这条主链同时也是标准派单演示的业务主线。

## 6. 订单状态机的业务含义

系统采用强约束订单状态机来保证业务顺序正确。主要状态及业务含义如下：

- `CREATED`：订单已创建，但尚未支付托管。
- `PAID_ESCROW`：需求方已支付托管，具备派单前提。
- `DISPATCH_PROPOSED`：Agent 已生成派单建议，但尚未人工确认。
- `DISPATCH_CONFIRMED`：调度员已确认派单结果。
- `IN_SERVICE`：农机正在执行作业。
- `ABNORMAL_PENDING`：出现天气、故障等异常，订单暂停等待处置。
- `REASSIGN_PROPOSED`：Agent 已给出改派建议。
- `REASSIGN_CONFIRMED`：调度员已确认改派结果。
- `TO_CONFIRM`：作业完成，等待需求方验收。
- `COMPLETED`：订单已验收完成。
- `REFUNDING`：订单进入退款流程。
- `REFUNDED`：退款完成。
- `DISPUTE`：进入争议处理。
- `CANCELED`：订单取消。

从业务上看，这套状态机的价值不只是“技术控制”，而是把交易履约过程拆成可解释、可审核、可追溯的阶段。

## 7. Agent 的业务定位

当前系统中的 Agent 不是独立替代人类的自动执行者，而是“智能决策辅助层”。它主要参与四类业务动作：

- 需求解析：把模糊需求转为结构化任务。
- 派单建议：基于多因素计算候选农机。
- 异常处置：对故障、天气、超时等情况给出处理建议。
- 决策解释：把系统建议翻译成业务可理解的语言。

同时，Agent 在当前版本有明确边界：

- 首次派单仍需要人工确认。
- 改派仍需要人工确认。
- 退款属于高风险动作，由人工明确触发。
- Agent 当前主要做“建议”和“辅助”，不是完全自动化裁决。

## 8. 标准派单演示的业务逻辑

标准派单演示展示的是“正常交易与正常履约”。

业务流程如下：

1. 需求方提出需求，例如“明天上午收割 300 亩小麦，预算 20000”。
2. Agent 识别出作业类型、面积、预算、紧急程度等关键信息。
3. 系统创建订单，订单进入 `CREATED`。
4. 需求方支付托管后进入 `PAID_ESCROW`。
5. Agent 根据距离、可用性、价格、口碑、天气风险等因素生成候选列表。
6. 调度员确认最终派单结果，订单进入 `DISPATCH_CONFIRMED`。
7. 农机开始作业，订单进入 `IN_SERVICE`。
8. 作业完成后进入 `TO_CONFIRM`。
9. 需求方确认验收，订单进入 `COMPLETED`。

这条链路体现的是：Agent 如何帮助交易从“需求输入”顺畅走到“服务完成”。

## 9. 天气暂停演示的业务逻辑

天气暂停演示展示的是“履约中断后的风险控制与恢复闭环”。

业务场景设定为：

- 订单已完成正常派单并开始作业。
- 当前执行农机所在区域天气良好。
- 但订单目的地突发暴雨或雷暴，高风险天气不适合继续作业。

业务逻辑如下：

1. 订单按标准流程进入 `IN_SERVICE`。
2. 系统检测到目的地天气风险高。
3. Agent 判断应优先保障作业安全，而不是机械性推进履约。
4. 订单进入 `ABNORMAL_PENDING`，即“暂停待处理”。
5. 后续有三种处理路径：
   - 天气复检后风险降低，恢复原机继续作业，返回 `IN_SERVICE`。
   - 若天气仍恶化且需要换资源，可以进一步走改派路径。
   - 若业务无法继续，则进入退款流程，走 `REFUNDING -> REFUNDED`。

这条演示的业务重点不是“天气接口”，而是说明系统具备了面向风险的运营能力：

- 作业安全优先于流程速度。
- 目的地天气优先于农机出发地天气。
- Agent 可以帮助调度员做暂停、复检和恢复判断。

## 10. 故障改派演示的业务逻辑

故障改派演示展示的是“履约中的资源失效恢复”。

业务场景设定为：

- 订单已完成首次派单并开始作业。
- 执行中的农机发生故障，无法继续履约。

业务逻辑如下：

1. 订单按标准流程进入 `IN_SERVICE`。
2. 故障发生后，订单进入 `ABNORMAL_PENDING`。
3. Agent 排除当前故障农机，重新计算可替补资源。
4. 系统给出改派建议，订单进入 `REASSIGN_PROPOSED`。
5. 调度员确认改派后，订单进入 `REASSIGN_CONFIRMED`。
6. 替补农机接单并恢复作业，回到 `IN_SERVICE`。
7. 后续继续完成作业、验收和完结。

这条演示的业务价值在于说明：系统并非只会“第一次派单”，也具备履约中断后的再规划能力。

## 11. 天气异常后的三种业务处理动作

在最新版本中，天气异常暂停后已经不是终点，而是完整闭环的一部分。当前前端暴露了三个明确的业务动作：

### 11.1 天气复检

业务含义：

- 调度员在暂停后重新检查订单目的地天气。
- 核心目的是确认“当前是否仍然不适合作业”。

适用场景：

- 暂停一段时间后，需要判断是否可以恢复执行。

### 11.2 恢复原机继续

业务含义：

- 当天气复检后风险已下降，且无需改派时，恢复原执行农机继续履约。

适用场景：

- 原机资源仍可用。
- 作业窗口重新打开。
- 系统不再建议暂停。

### 11.3 取消并退款

业务含义：

- 当天气持续恶化，业务无法继续推进时，终止订单并启动退款。

适用场景：

- 短期内无法恢复作业。
- 改派也无法有效解决问题。
- 业务方选择终止履约。

## 12. Agent 在业务中的核心价值

从业务层面总结，当前 Agent 的价值主要体现在以下五点：

- 降低下单理解成本：需求方不需要手工填写复杂表单。
- 提升派单效率：系统能快速给出候选和推荐结果。
- 增强异常响应能力：遇到天气或故障不需要完全依赖人工从零判断。
- 增强可解释性：让需求方、调度员知道“为什么这么做”。
- 提升演示与管理直观性：地图、天气面板、状态机和事件日志能完整展示业务过程。

## 13. 当前业务约束与边界

为了保证系统稳定可控，当前业务范围做了刻意收敛：

- 支付为模拟托管，不接真实支付网关。
- 天气异常中的自动暂停可走通，但高风险动作仍保留人工确认思路。
- 当前重点在“Agent 决策辅助”，不是全自动无人值守。
- 当前数据全部是模拟数据，用于验证逻辑与演示完整性。

## 14. 业务层结论

本系统当前已经跑通了一个完整的 Agent 驱动业务闭环：

- 正常场景可完成下单、派单、履约和验收。
- 天气场景可完成暂停、复检、恢复或退款。
- 故障场景可完成异常识别、替补推荐和改派续作。

从业务视角看，这个系统的意义不在于平台页面多少，而在于证明了 Agent 可以在农机服务电商中承担“理解、建议、协同、解释”四类关键职责。
